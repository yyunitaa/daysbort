# Only used locally by refresh/pull scripts (scripts/export-data.mjs,
# scripts/pull-followers.mjs). Not needed on Vercel — the deployed dashboard
# reads the committed snapshot files in data/*.js only.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=kanalytics_spi_test
DB_USER=postgres
DB_PASSWORD=postgres

# Used only by scripts/pull-followers.mjs (npm run pull-followers -- --yes).
# SPENDS REAL MONEY when used — see that script's header comment.
APIFY_TOKEN=
